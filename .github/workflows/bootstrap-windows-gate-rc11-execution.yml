name: RETIRED — Historical RC11 Windows gate

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: retired-bootstrap-windows-gate-rc11-execution
  cancel-in-progress: false

jobs:
  retired:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Fail closed
        run: |
          echo "This historical workflow is retired and cannot change repository or release state."
          exit 1
