name: RETIRED — ImageLab RC12 Windows Gate Execution

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Enforce retirement boundary
        run: |
          echo "RETIRED_WORKFLOW"
          echo "RC12 is superseded by exact RC13 evidence and the current zero-trust source gate."
          echo "This workflow must not be used for release authorization."
          exit 1
