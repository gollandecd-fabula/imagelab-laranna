name: RETIRED — Recovery PR Apply RC13

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Enforce retirement boundary
        run: |
          echo "RETIRED_WORKFLOW"
          echo "The RC13 patch was already applied and verified."
          echo "Historical patch and evidence remain in Git history."
          exit 1
