name: RETIRED — Diagnostic Update Rollback 1.4.8 to 1.4.9

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Enforce retirement boundary
        run: |
          echo "RETIRED_WORKFLOW"
          echo "Superseded by diagnostic-real-project-preservation-148-to-149.yml."
          echo "Historical evidence remains in Git history and recovery/evidence/update-rollback/."
          echo "This workflow cannot authorize G6 or G7."
          exit 1
