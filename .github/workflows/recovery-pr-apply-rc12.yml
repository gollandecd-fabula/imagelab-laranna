name: RETIRED — Recovery PR Apply RC12

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Enforce retirement boundary
        run: |
          echo "RETIRED_WORKFLOW"
          echo "The RC12 patch was already applied and superseded by RC13."
          echo "Historical patch and evidence remain in Git history."
          exit 1
