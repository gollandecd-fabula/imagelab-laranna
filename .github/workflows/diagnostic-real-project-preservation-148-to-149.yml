name: RETIRED — Historical 1.4.8 to 1.4.9 preservation diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: retired-diagnostic-real-project-preservation-148-to-149
  cancel-in-progress: false

jobs:
  retired:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Fail closed
        run: |
          echo "This historical workflow is retired and cannot change repository or release state."
          exit 1
