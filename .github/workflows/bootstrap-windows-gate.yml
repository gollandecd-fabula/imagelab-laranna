name: RETIRED — ImageLab Bootstrap Windows Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Enforce retirement boundary
        run: |
          echo "RETIRED_WORKFLOW"
          echo "The bootstrap RC11-era gate is superseded by exact RC13 evidence and the current zero-trust source workflow."
          echo "This historical workflow must not be used for release authorization."
          exit 1
