# Public repository policy

Do not commit credentials, private keys, personal data, user projects, commercial datasets, installer binaries, runtime logs, or unreviewed model candidates.

A GitHub Actions artifact is not a release until the Zero-Trust Gate emits `ImageLab-RELEASE-AUTHORIZED`.
