 def main() -> int:
@@ -120,145 +189,125 @@
     parser.add_argument("--output-dir", type=Path, default=ROOT / "dist")
     parser.add_argument("--label", default="ZERO_TRUST")
     parser.add_argument("--l3-runtime", type=Path)
-    parser.add_argument("--source-gate-receipt", type=Path)
     args = parser.parse_args()
 
-    # Fail before any build-side filesystem mutation. The default no-arg path
-    # still checks the current environment, while a release build must provide
-    # the hash-bound verified L3 runtime and a full-source PASS receipt.
     if args.l3_runtime is None:
         assert_source_test_runtime_capability()
         raise RuntimeError("verified L3 runtime is required before candidate build")
-    assert_source_test_runtime_capability(extra_runtime=args.l3_runtime)
-    if args.source_gate_receipt is None:
-        raise RuntimeError("full source-gate PASS receipt is required before candidate build")
-    validate_source_gate_receipt(
-        receipt_path=args.source_gate_receipt,
-        root=ROOT,
-        runtime_dir=args.l3_runtime,
-    )
-
-    output_dir = args.output_dir.resolve()
-    output_dir.mkdir(parents=True, exist_ok=True)
-    build_dir = ROOT / ".zero_trust_build"
-    shutil.rmtree(build_dir, ignore_errors=True)
-    build_dir.mkdir(parents=True)
-
-    if not BUNDLED_PILLOW_HEIF_WHEEL.is_file():
-        raise RuntimeError(f"mandatory bundled pillow-heif wheel missing: {BUNDLED_PILLOW_HEIF_WHEEL}")
-    actual_pillow_heif_sha = hashlib.sha256(BUNDLED_PILLOW_HEIF_WHEEL.read_bytes()).hexdigest()
-    if actual_pillow_heif_sha != BUNDLED_PILLOW_HEIF_SHA256:
-        raise RuntimeError(
-            "bundled pillow-heif wheel SHA-256 mismatch: "
-            f"expected {BUNDLED_PILLOW_HEIF_SHA256}, got {actual_pillow_heif_sha}"
-        )
-    if not BUNDLED_PSD_TOOLS_WHEEL.is_file():
-        raise RuntimeError(f"mandatory bundled psd-tools wheel missing: {BUNDLED_PSD_TOOLS_WHEEL}")
-    actual_psd_tools_sha = hashlib.sha256(BUNDLED_PSD_TOOLS_WHEEL.read_bytes()).hexdigest()
-    if actual_psd_tools_sha != BUNDLED_PSD_TOOLS_SHA256:
-        raise RuntimeError(
-            "bundled psd-tools wheel SHA-256 mismatch: "
-            f"expected {BUNDLED_PSD_TOOLS_SHA256}, got {actual_psd_tools_sha}"
-        )
-
-    if not BUNDLED_STET_EXE.is_file():
-        raise RuntimeError(f"mandatory bundled Stet EPS renderer missing: {BUNDLED_STET_EXE}")
-    actual_stet_sha = hashlib.sha256(BUNDLED_STET_EXE.read_bytes()).hexdigest()
-    if actual_stet_sha != BUNDLED_STET_SHA256:
-        raise RuntimeError(
-            "bundled Stet EPS renderer SHA-256 mismatch: "
-            f"expected {BUNDLED_STET_SHA256}, got {actual_stet_sha}"
-        )
-
-    if not BUNDLED_STET_LINUX.is_file():
-        raise RuntimeError(f"mandatory source-only Linux Stet EPS renderer missing: {BUNDLED_STET_LINUX}")
-    actual_stet_linux_sha = hashlib.sha256(BUNDLED_STET_LINUX.read_bytes()).hexdigest()
-    if actual_stet_linux_sha != BUNDLED_STET_LINUX_SHA256:
-        raise RuntimeError(
-            "source-only Linux Stet EPS renderer SHA-256 mismatch: "
-            f"expected {BUNDLED_STET_LINUX_SHA256}, got {actual_stet_linux_sha}"
-        )
-
-    identity = read_identity()
-    safe_label = "".join(ch if ch.isalnum() or ch in "-_" else "_" for ch in args.label)
-    installer_path = output_dir / f"ImageLab_by_LarannA_{safe_label}_Setup_x64.exe"
-    source_zip = output_dir / f"ImageLab_by_LarannA_{safe_label}_SOURCE.zip"
-    payload_path = ROOT / "windows_installer" / "installer" / "payload.zip"
-
-    launcher = build_dir / "ImageLab.exe"
-    uninstaller = build_dir / "Uninstall.exe"
-    build_go(ROOT / "windows_installer" / "launcher", launcher, gui=True)
-    build_go(ROOT / "windows_installer" / "uninstaller", uninstaller, gui=True)
-
-    payload_entries = [("ImageLab.exe", launcher.read_bytes()), ("Uninstall.exe", uninstaller.read_bytes())]
-    payload_entries.extend(
-        (path.relative_to(ROOT).as_posix(), path.read_bytes())
-        for path in ROOT.rglob("*") if payload_file(path)
-    )
